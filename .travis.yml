language: python
sudo: false
python:
    - 2.7
install:
    - pip install zc.buildout
    - buildout bootstrap
    - buildout
script:
    - bin/test --all -cv
notifications:
    email: false
cache:
  pip: true
  directories:
    - eggs/
    - develop/
