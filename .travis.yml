language: python
sudo: false
python:
    - 2.7
install:
    - pip install zc.buildout coverage coveralls
    - buildout bootstrap
    - buildout
script:
    - coverage run bin/test --all -cv
notifications:
    email: false
cache:
  pip: true
  directories:
    - eggs/
    - develop/
